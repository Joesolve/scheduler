[phases.build]
cmds = ["npm ci", "npx prisma generate", "npm run build"]

[start]
cmd = "npx prisma db push --accept-data-loss && npx tsx scripts/seed.ts && node scripts/migrate-events.js && next start"
